CC = gcc # compiler to be used

SRC = main.c school.c

OBJ = $(SRC:.c=.o) # substitutes the SRC to produce object files

NAME = school # name of the executable

RM = rm -f

all: ${OBJ}
	$(CC) $(OBJ) -o ${NAME}

clean:
	$(RM) *~ $(NAME)

oclean:
	${RM} ${OBJ}

fclean: clean oclean

re: fclean all
